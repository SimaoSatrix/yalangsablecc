fact (n: int) : int {
  f, i : int = 1;
  
  while i <= n do {
    f = f * i;
    i = i + 1;
  };
  
  return f;
};

main () : void {
  //print("Introduza um inteiro: ");
  x : int = input();
  //print(fact(x));
};